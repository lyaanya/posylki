-- audit_log создавался в первой миграции (E01, до того как RLS-by-default
-- стал стандартной практикой для всех новых таблиц, см. core_user_tables).
-- Закрываем тот же пробел здесь: без RLS журнал модераторских действий был
-- читаем напрямую через публичный anon-ключ, в обход бэкенда.
alter table audit_log enable row level security;
